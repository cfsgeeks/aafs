<ul class="topnav">
    <li class="dropdown">
        <a href="#">About Us</a>
        <ul class="dropdown-menu">
            {% setcontent aboutlinks = 'pages' where {section:"about"} %}
            {% for link in aboutlinks %}
            <li><a href="/{{link.slug}}">{{link.title}}</a></li>
            {% endfor %}
        </ul>
    </li>
    <li class="dropdown"></li>
    <li class="dropdown"></li>
    <li class="dropdown"></li>
</ul>