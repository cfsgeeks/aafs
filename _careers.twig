{% setcontent careeritems = '/careers/latest/5' %}
{% if careeritems|length > 0 %}
<ul class="unstyled">
    {% for item in careeritems %}
    <li>
        <a href="{{item.link()}}">{{item.title}}</a>
        <p><small>in {{item.location}}</small></p>
        <p>{{item.description}}</p>
    </li>
    {% endfor %}
</ul>
{% else %}
<p>We're always seeking individuals interested in the exciting field of social services.</p>
<p>If this sounds like you, please <a href="/work-with-us/">submit your resume</a> today.</p>
{% endif %}