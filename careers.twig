{% block contenttitle %}Current Job Openings{% endblock %}
{% extends "_base.twig" %}
{% block main %}
<table class="table table-striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Location</th>
            <th>Term</th>
            <th>Closing Date</th>
        </tr>
    </thead>
    <tbody>
        {% for record in records %}
        <tr>
            <td><a href="/{{record.contenttype.singular_name}}/{{record|slug}}">{{record.title}}</td>
            <td>{{record.location}}</td>
            <td>{{record.term}}</td>
            <td></td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
